#include "main.h"

/**
 * add - Takes two integers from arguments and adds them 
 * together
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
